# npm registry mirror.
# This environment's network blocks the default registry (registry.npmjs.org),
# so we use the public yarnpkg mirror, which serves identical npm packages.
# Safe to remove this file if installing from a network that can reach npmjs.org.
registry=https://registry.yarnpkg.com
